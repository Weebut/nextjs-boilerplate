import { Layout } from '@components/layouts/layout.component';
import { Strip } from '@components/strips/strip.component';

export function FileUploadContainer() {
  return (
    <Layout>
      <Strip>File upload</Strip>
    </Layout>
  );
}
